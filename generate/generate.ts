import * as co from "co"
import * as chalk from "chalk"

export default function generate(item: string, location: string, doInject: boolean) {
    return new Promise((resolve, reject) => {
        
    })
}